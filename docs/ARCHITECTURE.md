# 🏛️ Freebuff-Power Architecture & Swarm Topology

```mermaid
graph TD
    User([User Developer]) --> CLI[freebuff-power CLI v4.5]
    
    subgraph Core Engine
        CLI --> TUI[Interactive TUI Dashboard]
        CLI --> Composer[Power Composer 2.0]
        CLI --> Vault[Multi-Account Vault & Rotator]
        CLI --> Sentinel[Anti-Ban Telemetry Shield]
    end
    
    subgraph Multi-Agent Swarm
        Composer --> Arch[@architect & @planner]
        Arch --> Dev[@programmer & 44 Specialized Agents]
        Dev --> QA[@tddmaster & @qa]
        QA --> Sec[@security & @reviewer]
    end
    
    subgraph Modular Skills Catalog
        Dev -.-> S1[1,061 Modular Skills]
        QA -.-> S1
    end
    
    subgraph Developer Tooling
        CLI --> Snapshots[Time-Travel Checkpoints]
        CLI --> Tunnel[Mobile Preview Tunnel]
        CLI --> Audit[Security & Slop Scanner]
        CLI --> Scaffolder[Project Scaffolder]
        CLI --> Exporter[Multi-IDE Exporter]
    end
```

## 1. Multi-Agent Swarm Governance
Freebuff-Power coordinates 46 specialized sub-agents via structured handoff artifacts:
1. `RFC.md` — Interface contracts and DDL generated by `@architect`.
2. `DIFF_LOG.md` — Changes executed by `@programmer`.
3. `TEST_REPORT.md` — Verification tests run by `@tddmaster` / `@qa`.
4. `SECURITY_PASS.md` — OWASP audits signed off by `@security`.
5. `MEMORY.md` — State synchronization persisted by `[Skill: memory-by-ferz]`.
