# Bento Grid Asymmetric Layout Architecture

- CSS Grid `grid-cols-1 md:grid-cols-3 lg:grid-cols-4`
- Span combinations: `col-span-2 row-span-2` for hero card
- Subtle borders `border border-white/10` with gradient sheen
