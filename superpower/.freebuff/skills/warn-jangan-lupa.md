---
name: warn-jangan-lupa
description: warn "JANGAN LUPA: isi API key di $INSTALL_DIR/.env" — skill otomatis dari sesi nyata
---

# warn-jangan-lupa

**Tujuan:** warn "JANGAN LUPA: isi API key di $INSTALL_DIR/.env"

**Tools yang dipakai:** terminal, find_files, grep, read_file, write_file

**Langkah yang terbukti berhasil** (dari sesi nyata):

1. pakai tool `terminal`
2. pakai tool `find_files`
3. pakai tool `grep`
4. pakai tool `read_file`
5. pakai tool `write_file`

