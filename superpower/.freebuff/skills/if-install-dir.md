---
name: if-install-dir
description: if [ -f "$INSTALL_DIR/.env" ] && ! grep -q "=" "$INSTALL_DIR/.env"; th — skill otomatis dari sesi nyata
---

# if-install-dir

**Tujuan:** if [ -f "$INSTALL_DIR/.env" ] && ! grep -q "=" "$INSTALL_DIR/.env"; then

**Tools yang dipakai:** terminal, write_file, read_file, grep

**Langkah yang terbukti berhasil** (dari sesi nyata):

1. pakai tool `terminal`
2. pakai tool `write_file`
3. pakai tool `read_file`
4. pakai tool `grep`

